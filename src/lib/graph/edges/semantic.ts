// src/lib/graph/edges/semantic.ts (stub; real impl in the semantic task)
import type { BuildOpts, GraphEdge, MemoryRecord } from "../types.js";
export function semanticEdges(
	_mems: MemoryRecord[],
	_opts: BuildOpts,
): GraphEdge[] {
	return [];
}
